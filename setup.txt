# Setup
for the initial setup run the following command in CC
```
openp/github run TheRealRazbi 1710_pack_lua main setup.lua
```

for updating the existing code, run "/pull" (which is created by setup.lua in the previous step)

# Run
run the command in the root of CC
```
main
```
